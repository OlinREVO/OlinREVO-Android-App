<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="RSpeedometer">
        <attr name="maxSpeed" format="float"></attr>
        <attr name="currentSpeed" format="float"></attr>
        <attr name="onColor" format="color"></attr>
        <attr name="offColor" format="color"></attr>
        <attr name="scaleColor" format="color"></attr>
        <attr name="scaleTextSize" format="dimension"></attr>
        <attr name="readingTextSize" format="dimension"></attr>
    </declare-styleable>

    <declare-styleable name="RBatteryMeter">
        <attr name="maxCharge" format="float"></attr>
        <attr name="currentCharge" format="float"></attr>
        <attr name="batteryOnColor" format="color"></attr>
        <attr name="batteryOffColor" format="color"></attr>
        <attr name="batteryScaleColor" format="color"></attr>
        <attr name="batteryScaleTextSize" format="dimension"></attr>
        <attr name="batteryReadingTextSize" format="dimension"></attr>
    </declare-styleable>

</resources>
